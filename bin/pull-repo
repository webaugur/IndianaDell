#!/usr/bin/env bash
# Pull IndianaDell + LFS (+ optional DragonSDR / verify / docs).
# Full sync:  bin/pull-repo --dragonsdr --verify --build-docs
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec "$ROOT/scripts/github/pull-all.sh" "$@"
