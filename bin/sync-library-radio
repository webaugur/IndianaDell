#!/usr/bin/env bash
set -euo pipefail
# Resolve symlinks so PATH install (~/.local/bin → repo/bin) still finds scripts/
SELF="$(readlink -f "$0")"
ROOT="$(cd "$(dirname "$SELF")/.." && pwd)"
exec "$ROOT/scripts/library/sync-library-radio.sh" "$@"
