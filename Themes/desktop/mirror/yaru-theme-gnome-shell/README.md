# Mirror: yaru-theme-gnome-shell (desktop)

Same package as `login/mirror/yaru-theme-gnome-shell/` — duplicated here because `yaru-theme-gnome-shell` is listed in both **login** and **desktop** `apt-packages.txt`.

Controls panel, quick settings, notifications, and app grid **after** login (same files as greeter).

See `../../../login/mirror/yaru-theme-gnome-shell/README.md` for asset list and edit instructions.